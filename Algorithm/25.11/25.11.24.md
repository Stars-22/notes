# 25.11.24 算法刷题笔记

## 力扣：17. 电话号码的字母组合
**链接**：https://leetcode.cn/problems/letter-combinations-of-a-phone-number/  
**相关标签**：`哈希表` `字符串` `回溯`  
**难度**：`中等`
### 方法1：`深度优先搜索`
- **时间复杂度**：$O(3^m * 4^n)$
- **空间复杂度**：$O(m+n)$
```cpp
class Solution {
public:
    vector<string> letterCombinations(string digits) {
        string s;
        vector<string> ans;
        const string m[8] = {"abc", "def", "ghi", "jkl", "mno", "pqrs", "tuv", "wxyz"};
        auto dfs = [&](this auto&& dfs, int i) -> void {
            if(i == digits.size()) ans.push_back(s);
            else for(const auto& m : m[digits[i] - '2']){
                s += m;
                dfs(i+1);
                s.pop_back();
            }
        };
        dfs(0);
        return ans;
    }
};
```

## 力扣：114. 二叉树展开为链表
**链接**：https://leetcode.cn/problems/flatten-binary-tree-to-linked-list/  
**相关标签**：`栈` `树` `深度优先搜索` `链表` `二叉树`  
**难度**：`中等`
### 方法1：`Morris遍历算法`
- **时间复杂度**：$O(n)$
- **空间复杂度**：$O(1)$
```cpp
class Solution {
public:
    void flatten(TreeNode* root) {
        while(root){
            if(root->left){
                TreeNode* temp = root->left;
                while(temp->right) temp = temp->right;
                temp->right = root->right;
                root->right = root->left;
                root->left = nullptr;
            }
            root = root->right;
        }
    }
};
```